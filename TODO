body-parser --done

morgan --done

Handling CORS --done

Add mongodb Atlas --done

Add mongoose integration --done

Update orders.js dummy code --done

Better responses in products.js --done

multer for image upload --done

User login? --done

JSON Web Token(JWT) signing --done

Add route protection --done

A user who is authenticated can delete another users orders, needs fixing!!
